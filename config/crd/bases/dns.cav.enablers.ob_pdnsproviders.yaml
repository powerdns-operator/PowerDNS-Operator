---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: pdnsproviders.dns.cav.enablers.ob
spec:
  group: dns.cav.enablers.ob
  names:
    kind: PDNSProvider
    listKind: PDNSProviderList
    plural: pdnsproviders
    singular: pdnsprovider
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.url
      name: URL
      type: string
    - jsonPath: .status.connectionStatus
      name: Connection Status
      type: string
    - jsonPath: .status.powerDNSVersion
      name: PowerDNS Version
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha3
    schema:
      openAPIV3Schema:
        description: PDNSProvider is the Schema for the pdnsproviders API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of PDNSProvider
            properties:
              credentials:
                description: Credentials defines credentials configuration for PowerDNS
                  API
                properties:
                  secretRef:
                    description: SecretRef is a reference to a Kubernetes Secret containing
                      the PowerDNS API key
                    properties:
                      key:
                        default: apiKey
                        description: Key is the key in the Secret containing the API
                          key
                        type: string
                      name:
                        description: Name is the name of the Secret
                        type: string
                      namespace:
                        description: |-
                          Namespace is the namespace of the Secret
                          If not specified, defaults to the PDNSProvider resource namespace
                        type: string
                    required:
                    - name
                    type: object
                required:
                - secretRef
                type: object
              interval:
                default: 5m
                description: Interval is the reconciliation interval to check the
                  connection to the PowerDNS API
                type: string
              proxy:
                description: |-
                  Proxy is the URL of the HTTP/HTTPS proxy to use for connecting to PowerDNS API
                  Format: http://proxy.example.com:8080 or https://proxy.example.com:8080
                type: string
              timeout:
                default: 10s
                description: Timeout is the timeout for PowerDNS API requests, defaults
                  to 10s
                type: string
              tls:
                description: TLS defines TLS configuration for PowerDNS API connection
                properties:
                  caBundleRef:
                    description: CABundleRef is a reference to a ConfigMap or Secret
                      containing a CA bundle
                    properties:
                      key:
                        default: ca.crt
                        description: Key is the key in the ConfigMap or Secret containing
                          the CA bundle
                        type: string
                      kind:
                        default: ConfigMap
                        description: Kind is the kind of resource (ConfigMap or Secret)
                        enum:
                        - ConfigMap
                        - Secret
                        type: string
                      name:
                        description: Name is the name of the ConfigMap or Secret
                        type: string
                      namespace:
                        description: |-
                          Namespace is the namespace of the ConfigMap or Secret
                          If not specified, defaults to the operator namespace
                        type: string
                    required:
                    - name
                    type: object
                  insecure:
                    default: false
                    description: Insecure enables insecure connections to PowerDNS
                      API (skip TLS verification)
                    type: boolean
                type: object
              url:
                description: URL is the URL of the PowerDNS API
                pattern: ^https?://.*
                type: string
              vhost:
                default: localhost
                description: Vhost is the vhost/server ID of the PowerDNS API, defaults
                  to "localhost"
                type: string
            required:
            - credentials
            - url
            type: object
          status:
            description: status defines the observed state of PDNSProvider
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the PDNSProvider's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              connectionStatus:
                description: ConnectionStatus indicates the status of the connection
                  to the PowerDNS API
                type: string
              daemonType:
                description: DaemonType is the type of PowerDNS daemon (should be
                  "authoritative")
                type: string
              lastConnectionTime:
                description: LastConnectionTime is the last time a successful connection
                  was established
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  for this PDNSProvider
                format: int64
                type: integer
              powerDNSVersion:
                description: PowerDNSVersion is the version of the connected PowerDNS
                  server
                type: string
              serverID:
                description: ServerID is the ID of the PowerDNS server
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
